[project]
name = "brains"
version = "0.1.0"
description = "Behavioral Reasoning & Adaptive Intelligence Network Swarm: six local review agents, each checking text from a different angle"
readme = "README.md"
authors = [
    { name = "Aalok Pandit", email = "paalok333@gmail.com" }
]
requires-python = ">=3.11"
dependencies = []

[project.scripts]
brains = "brains:main"

[build-system]
requires = ["uv_build>=0.12.17,<0.13.0"]
build-backend = "uv_build"
